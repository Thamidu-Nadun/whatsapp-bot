// app configurations
const appName = "Whatsapp Bot";
const UserName = "NadSoft";
const Mode = "online";

// functions configurations
const ImageLimit = 5;

// Export the configurations
module.exports = {
  appName,
  UserName,
  Mode,
  ImageLimit,
};